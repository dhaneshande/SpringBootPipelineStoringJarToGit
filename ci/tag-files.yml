---
platform: linux

image_resource:
  type: docker-image
  source: {repository: concourse/bosh-cli}

inputs:
- name: SpringBootSimpleRest
- name: built-project

run:
  path: sh
  args:
    - -exc
    - |
      ls -l
      pwd
      ls -R
      git clone SpringBootSimpleRest built-project
      cd built-project
      ls -la
      git config --global user.email "dande@miraclesoft.com"
      git config --global user.name "dhaneshande"
      #git init
      git add .
      git commit -m "Bumped date"
      #git push origin https://github.com/dhaneshande/SpringBootPipelineStoringJarToGit.git
